# Walker 奖励函数说明

这份文档解释 `run.py` 中的奖励函数。当前训练代码不使用 `dm_control` 返回的 `ts.reward`，而是直接调用 `run.py` 里的本地奖励函数，方便阅读、修改和教学。

## 1. 奖励函数在哪里

奖励代码在 `run.py` 的 `Reward Functions` 区块，主要包括：

```python
WALKER_STAND_HEIGHT = 1.3
WALKER_STAND_MARGIN = 0.35
WALKER_WALK_SPEED = 2.0
WALKER_RUN_SPEED = 8.0
WALKER_MAX_KNEE_BEND = 2.0
WALKER_KNEE_MARGIN = 0.6
WALKER_MAX_VERTICAL_SPEED = 1.0
WALKER_VERTICAL_SPEED_MARGIN = 2.0
WALKER_MIN_LEG_ACTIVITY_RATIO = 0.25
WALKER_LEG_ACTIVITY_RATIO_MARGIN = 0.25
WALKER_MIN_FOOT_SEPARATION = 0.15
WALKER_MAX_FOOT_SEPARATION = 0.9
WALKER_FOOT_SEPARATION_MARGIN = 0.4
WALKER_MIN_ALTERNATION_PROGRESS = 0.01
WALKER_ALTERNATION_PROGRESS_MARGIN = 0.08
WALKER_ALTERNATION_WEIGHT = 0.8
WALKER_FRONT_FOOT_THRESHOLD = 0.12
WALKER_CATCHUP_REWARD_WEIGHT = 0.2
WALKER_SWITCH_REWARD_WEIGHT = 0.5
WALKER_FRONT_BALANCE_REWARD_WEIGHT = 0.3
WALKER_UPRIGHT_QPOS = {
    "rootz": 0.0,
    "rootx": 0.0,
    "rooty": 0.0,
    "right_hip": 0.0,
    "right_knee": 0.0,
    "right_ankle": 0.0,
    "left_hip": 0.0,
    "left_knee": 0.0,
    "left_ankle": 0.0,
}

def tolerance(...):
    ...

def walker_reward(physics, task, previous_state=None, gait_state=None):
    ...

def compute_reward(env, domain, task, previous_state=None, gait_state=None):
    ...
```

训练时真正使用奖励的位置是：

```python
previous_reward_state = capture_reward_state(env, args.domain)
next_ts = env.step(action_env)
reward = compute_reward(
    env,
    args.domain,
    args.task,
    previous_state=previous_reward_state,
    gait_state=gait_state,
)
replay_buffer.add(obs, action_norm, reward, next_obs, done)
```

也就是说：

1. `dm_control` 负责物理仿真，执行动作并更新环境状态。
2. `run.py` 从 `env.physics` 中读取机器人当前状态。
3. `run.py` 自己计算 reward。
4. 这个 reward 被存入 replay buffer，之后用于 TD3 训练。

因此，学生如果想改变智能体的学习目标，应该改 `run.py` 里的 `walker_reward()` 或相关常量。

## 2. 初始姿态在哪里设置

`dm_control` 的 walker 在 `reset()` 时会随机化关节角度，所以有时看起来像从空中倒着掉下来。为了教学更直观，`run.py` 在每次 reset 后都会把机器人改成正立姿态：

```python
ts, obs = reset_env(env, args.domain)
```

核心代码是：

```python
def set_walker_upright_pose(physics):
    for joint_name, value in WALKER_UPRIGHT_QPOS.items():
        physics.named.data.qpos[joint_name] = value

    for joint_name in physics.named.data.qvel.axes.row.names:
        physics.named.data.qvel[joint_name] = 0.0

    physics.forward()
```

其中 `WALKER_UPRIGHT_QPOS` 把身体角度 `rooty`、髋关节、膝盖、脚踝都设为 `0.0`。这对应 XML 默认的正立姿态：躯干高度约为 `1.3`，身体朝上，速度为零。

因为我们手动改了物理状态，所以还会重新构造 observation：

```python
def walker_observation(physics):
    return {
        "orientations": physics.named.data.xmat[1:, ["xx", "xz"]].ravel(),
        "height": physics.named.data.xpos["torso", "z"],
        "velocity": physics.velocity(),
    }
```

训练、评估和录像都会使用 `reset_env()`，所以看到的初始姿态是一致的。

## 3. 为什么要改奖励

原始 walker 奖励主要鼓励：

```text
身体高 + 身体直 + 水平速度快
```

但实际训练中可能出现一种投机策略：机器人弹几下，然后跪着蹭地前进。它不是真正自然地走路，却仍然能靠水平速度拿到奖励。

所以新版奖励额外加入几个目标：

1. 膝盖不要长期弯得太深，减少跪着走。
2. 躯干不要上下速度太大，减少反复弹跳。
3. 左右腿都要参与动作，减少一条腿拖着另一条腿走。
4. 两条腿要前后交替，减少后腿一直推着前腿走。

新版奖励的核心思想是：

```text
reward = 站姿奖励 * 膝盖奖励 * 平稳奖励 * 步态奖励 * 速度奖励
```

## 4. 奖励函数使用了哪些物理量

`walker_reward()` 会读取这些物理量：

```python
torso_height = physics_value(physics.named.data.xpos["torso", "z"])
torso_upright = physics_value(physics.named.data.xmat["torso", "zz"])
horizontal_velocity = physics_value(
    physics.named.data.sensordata["torso_subtreelinvel"][0]
)
right_knee = abs(physics_value(physics.named.data.qpos["right_knee"]))
left_knee = abs(physics_value(physics.named.data.qpos["left_knee"]))
vertical_speed = abs(physics_value(physics.named.data.qvel["rootz"]))
right_activity = leg_activity(physics, "right")
left_activity = leg_activity(physics, "left")
current_foot_diff = foot_diff(physics)
```

含义如下：

| 变量 | 含义 | 作用 |
| --- | --- | --- |
| `torso_height` | 躯干高度 | 判断机器人是否站起来 |
| `torso_upright` | 躯干竖直程度 | 判断机器人身体是否直立 |
| `horizontal_velocity` | 水平前进速度 | 判断机器人是否走得快或跑得快 |
| `right_knee`, `left_knee` | 两条腿膝盖弯曲程度 | 判断是否过度跪膝 |
| `vertical_speed` | 躯干上下方向速度 | 判断是否在反复弹跳 |
| `right_activity`, `left_activity` | 两条腿关节速度总和 | 判断是否只有一条腿在动 |
| `current_foot_diff` | 左脚 x 位置减右脚 x 位置 | 判断脚距，以及哪只脚在前 |

为了判断“是否交替”，奖励还会在 `env.step()` 前记录一次脚的位置：

```python
previous_reward_state = capture_reward_state(env, args.domain)
```

然后在 `env.step()` 后比较前后两帧的 `foot_diff`。这是一个 transition reward：奖励不只看当前状态，也看“从上一步到这一步发生了什么”。

## 5. tolerance 函数的作用

`tolerance()` 是一个“软判断”函数。

普通判断可能是：

```python
if height >= 1.3:
    reward = 1
else:
    reward = 0
```

这种奖励太硬。比如高度 `1.29` 和 `0.2` 都会得到 `0`，但它们明显不是同样糟糕。

`tolerance()` 的思想是：

- 如果变量进入目标范围，返回 `1.0`。
- 如果变量离目标范围不远，返回一个介于 `0` 和 `1` 之间的值。
- 离目标越远，返回值越小。

这让奖励更加平滑，智能体在学习早期也能得到有用反馈。

## 6. 站姿奖励 stand_reward

站姿奖励由高度和直立程度相乘得到：

```python
standing = tolerance(
    torso_height,
    bounds=(WALKER_STAND_HEIGHT, float("inf")),
    margin=WALKER_STAND_MARGIN,
)

upright = (1.0 + torso_upright) / 2.0

stand_reward = standing * upright
```

含义是：

- `torso_height >= 1.3` 时，高度奖励较高。
- `torso_upright` 越接近 `1`，身体越竖直。
- 使用乘法意味着“高”和“直”必须同时满足。

原来的加权平均奖励允许某一项差一点时仍然拿到不少分。现在改成乘法后，跪着、趴着、歪着都会明显亏分。

## 7. 膝盖奖励 knee_reward

为了减少跪着走，新版奖励读取两个膝盖角度：

```python
right_knee = abs(physics_value(physics.named.data.qpos["right_knee"]))
left_knee = abs(physics_value(physics.named.data.qpos["left_knee"]))
max_knee_bend = max(right_knee, left_knee)
```

然后计算：

```python
knee_reward = tolerance(
    max_knee_bend,
    bounds=(0.0, WALKER_MAX_KNEE_BEND),
    margin=WALKER_KNEE_MARGIN,
    value_at_margin=0.2,
    sigmoid="linear",
)
```

walker 的膝盖关节大致范围是 `[-2.62, 0]`。绝对值越大，膝盖弯得越深。这里允许一定弯曲，但如果长期接近最大弯曲，奖励会下降。

调参建议：

- 仍然跪着走：把上限 `2.0` 改成 `1.8`。
- 变得太僵硬、不敢迈步：把上限 `2.0` 改成 `2.2`。

## 8. 平稳奖励 smooth_reward

为了减少“弹两下再跪着走”，新版奖励加入躯干竖直速度：

```python
vertical_speed = abs(physics_value(physics.named.data.qvel["rootz"]))
```

然后计算：

```python
smooth_reward = tolerance(
    vertical_speed,
    bounds=(0.0, WALKER_MAX_VERTICAL_SPEED),
    margin=WALKER_VERTICAL_SPEED_MARGIN,
    value_at_margin=0.2,
    sigmoid="linear",
)
```

含义是：

- 小幅上下运动可以接受。
- 躯干 z 方向速度太大时，说明动作可能在弹跳，奖励会下降。

调参建议：

- 仍然明显弹跳：把 `bounds=(0.0, 1.0)` 改成 `bounds=(0.0, 0.7)`。
- 动作太保守：把 `1.0` 放宽到 `1.2`。

## 9. posture_reward

新版姿态奖励是：

```python
posture_reward = stand_reward * knee_reward * smooth_reward
```

这三个奖励使用乘法，表示它们都很重要：

- 站不高，奖励低。
- 身体不直，奖励低。
- 膝盖弯太深，奖励低。
- 上下弹得太厉害，奖励低。

这种写法会减少奖励漏洞，让机器人更难通过跪走或弹跳拿到高分。

## 10. 步态奖励 gait_reward

如果机器人变成“一条腿拖着另一条腿走”，说明它找到了新的奖励漏洞：只要一条腿提供推进，另一条腿即使长期偷懒，也可能得到速度奖励。

新版奖励给 `walk/run` 加了三个步态约束。

### 10.1 左右腿活动平衡

代码会分别计算左右腿三个关节的速度总和：

```python
def leg_activity(physics, side):
    return sum(
        abs(physics_value(physics.named.data.qvel[f"{side}_{joint}"]))
        for joint in ("hip", "knee", "ankle")
    )
```

然后计算较小活动量和较大活动量的比例：

```python
leg_activity_ratio = min_leg_activity / (max_leg_activity + 1e-6)
```

如果只有一条腿在动，这个比例会接近 `0`。如果两条腿都在参与，比例会更高。

```python
leg_balance_reward = tolerance(
    leg_activity_ratio,
    bounds=(WALKER_MIN_LEG_ACTIVITY_RATIO, float("inf")),
    margin=WALKER_LEG_ACTIVITY_RATIO_MARGIN,
    value_at_margin=0.3,
    sigmoid="linear",
)
```

这里不是要求左右腿每一帧完全对称，而是防止一条腿长期几乎不动。

### 10.2 脚距约束

代码还会读取两只脚的水平距离：

```python
current_foot_diff = foot_diff(physics)
foot_separation = abs(current_foot_diff)
```

然后要求脚距在一个合理范围内：

```python
foot_separation_reward = tolerance(
    foot_separation,
    bounds=(WALKER_MIN_FOOT_SEPARATION, WALKER_MAX_FOOT_SEPARATION),
    margin=WALKER_FOOT_SEPARATION_MARGIN,
    value_at_margin=0.3,
    sigmoid="linear",
)
```

这个项有两个作用：

- 脚距太小：容易变成单脚跳或两腿黏在一起。
- 脚距太大：容易变成一条腿拖在后面。

### 10.3 前后交替约束

如果后腿一直推着前腿走，那么“哪只脚在前”长期不会变。为了解决这个问题，代码会比较 step 前后的 `foot_diff`，并维护一个 episode 内的 `gait_state`：

```python
def foot_diff(physics):
    return foot_x(physics, "left") - foot_x(physics, "right")
```

`foot_diff` 的符号表示哪只脚在前：

- `foot_diff > WALKER_FRONT_FOOT_THRESHOLD`：左脚明确在前。
- `foot_diff < -WALKER_FRONT_FOOT_THRESHOLD`：右脚明确在前。
- 中间区域不判定前脚，避免两只脚靠太近时靠抖动刷交替奖励。

训练循环会在 `env.step()` 前保存：

```python
previous_reward_state = capture_reward_state(env, args.domain)
```

step 后计算当前的 `current_foot_diff`，先得到一个小权重的追赶奖励：

```python
foot_diff_delta = current_foot_diff - previous_foot_diff
rear_catchup_progress = -np.sign(previous_foot_diff) * foot_diff_delta
```

追赶奖励只是 shaping，权重较小：

```python
WALKER_CATCHUP_REWARD_WEIGHT = 0.2
```

真正主要的交替奖励来自两个量：

```python
WALKER_SWITCH_REWARD_WEIGHT = 0.5
WALKER_FRONT_BALANCE_REWARD_WEIGHT = 0.3
```

- `switch_reward`：当前明确在前的脚和上一次明确在前的脚不同，说明前后脚完成了一次交换。
- `front_balance_reward`：统计当前 episode 里左脚在前和右脚在前的步数，比例越接近，奖励越高。

对应代码是：

```python
alternation_reward = alternating_gait_reward(
    current_foot_diff,
    previous_foot_diff,
    gait_state=gait_state,
)
```

这个版本不再只奖励“后脚追上前脚”。如果机器人只是后脚追上又退回，而始终不超过，`switch_reward` 和 `front_balance_reward` 会很低，因此拿不到高交替分。

最终步态奖励为：

```python
alternation_factor = (
    1.0
    - WALKER_ALTERNATION_WEIGHT
    + WALKER_ALTERNATION_WEIGHT * alternation_reward
)
gait_reward = leg_balance_reward * foot_separation_reward * alternation_factor
```

这个奖励只用于 `walk/run`，不影响 `stand`。

调参建议：

- 还是单腿拖行：把 `WALKER_MIN_LEG_ACTIVITY_RATIO` 从 `0.25` 提高到 `0.35`。
- 动作变得太僵硬：把 `0.25` 降到 `0.15`。
- 脚拖得很远：把 `WALKER_MAX_FOOT_SEPARATION` 从 `0.9` 降到 `0.75`。
- 前后脚还是不交换：把 `WALKER_SWITCH_REWARD_WEIGHT` 从 `0.5` 提高到 `0.6`，同时把 `WALKER_CATCHUP_REWARD_WEIGHT` 从 `0.2` 降到 `0.1`。
- 长期一只脚在前：把 `WALKER_FRONT_BALANCE_REWARD_WEIGHT` 从 `0.3` 提高到 `0.4`。
- 交替动作太急促：把 `WALKER_ALTERNATION_WEIGHT` 从 `0.8` 降到 `0.6`。

## 11. stand / walk / run 的区别

对于 `stand`：

```python
return float(posture_reward)
```

它只要求姿态好，不要求向前移动。

对于 `walk` 和 `run`，先设置目标速度：

```python
WALKER_WALK_SPEED = 2.0
WALKER_RUN_SPEED = 8.0
```

然后计算速度奖励：

```python
move_reward = tolerance(
    horizontal_velocity,
    bounds=(move_speed, float("inf")),
    margin=move_speed / 2.0,
    value_at_margin=0.5,
    sigmoid="linear",
)
```

最终奖励：

```python
reward = posture_reward * gait_reward * (0.2 + 0.8 * move_reward)
```

这个公式表示：

- 姿态是基础，姿态差时速度再快也拿不到高分。
- 两条腿都要参与，单腿拖行会降低 `gait_reward`。
- 后脚要逐步追上并交换到前面，否则 `alternation_reward` 会降低。
- 速度仍然重要，达到目标速度时可以拿满速度部分。
- 即使速度暂时不够，只要姿态好，也能保留 `20%` 的速度系数，帮助智能体先学站稳，再学走快。

## 12. 奖励如何影响 TD3 学习

训练循环中的关键步骤是：

```python
previous_reward_state = capture_reward_state(env, args.domain)
next_ts = env.step(action_env)
next_obs = flatten_obs(next_ts.observation)

reward = compute_reward(
    env,
    args.domain,
    args.task,
    previous_state=previous_reward_state,
    gait_state=gait_state,
)
done = float(next_ts.last())

replay_buffer.add(obs, action_norm, reward, next_obs, done)
```

每一步训练都会保存一条经验：

```text
(当前状态, 动作, 奖励, 下一个状态, 是否结束)
```

TD3 更新 critic 时，会用这个奖励计算目标 Q 值：

```python
target = rewards + gamma * (1.0 - dones) * target_q
```

因此：

- 奖励越高，critic 越倾向于认为这个动作好。
- actor 会学习选择 critic 认为 Q 值更高的动作。
- 修改奖励函数，就等于修改“什么行为是好行为”的定义。

## 13. 可以尝试的课堂实验

### 实验 1：观察去掉 knee_reward 的效果

把：

```python
posture_reward = stand_reward * knee_reward * smooth_reward
```

临时改成：

```python
posture_reward = stand_reward * smooth_reward
```

观察机器人是否更容易跪着走。

### 实验 2：观察去掉 smooth_reward 的效果

临时改成：

```python
posture_reward = stand_reward * knee_reward
```

观察机器人是否更容易出现上下弹跳。

### 实验 3：让 walk 任务更快

当前设置：

```python
WALKER_WALK_SPEED = 2.0
```

可以改成：

```python
WALKER_WALK_SPEED = 3.0
```

观察训练是否更难，以及最终动作是否更激进。

### 实验 4：观察去掉 gait_reward 的效果

临时把：

```python
return float(posture_reward * gait_reward * (0.2 + 0.8 * move_reward))
```

改成：

```python
return float(posture_reward * (0.2 + 0.8 * move_reward))
```

观察是否更容易出现一条腿拖着另一条腿走。

### 实验 5：观察去掉 alternation_reward 的效果

临时把：

```python
gait_reward = leg_balance_reward * foot_separation_reward * alternation_factor
```

改成：

```python
gait_reward = leg_balance_reward * foot_separation_reward
```

观察是否更容易出现后腿一直推着前腿、前脚身份不交换。

## 14. 总结

当前奖励函数的核心思想是：

```text
stand: 站高 + 站直 + 不跪太深 + 不乱弹
walk: 站高 + 站直 + 不跪太深 + 不乱弹 + 两条腿都参与 + 前后交替 + 达到 2.0 的前进速度
run:   站高 + 站直 + 不跪太深 + 不乱弹 + 两条腿都参与 + 前后交替 + 达到 8.0 的前进速度
```

强化学习不是直接告诉机器人“该怎么走”，而是通过奖励告诉它“什么结果更好”。奖励函数就是任务目标的代码化表达。
