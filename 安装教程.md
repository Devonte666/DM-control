README
=====

简要
--
本仓库包含一个基于 TD3 的教学示例（run.py），用于在 DeepMind Control Suite（dm_control）的 walker 任务上训练/评估智能体。下面提供在本地 Linux Ubuntu（推荐使用）或 Windows 上配置运行环境的最小步骤与常见问题解决建议。

基本要求
--
- 操作系统：推荐 Linux Ubuntu （Windows 原生对 `dm_control` 支持有限）。若在 Windows 原生运行，可能报错需要额外配置 OpenGL / MuJoCo（也跑通了问题不大）。
- Python 版本：3.10 或 3.11
- 电脑要求：本项目CPU就可以训练，对显卡要求不高。

推荐安装方式
--
1. 安装软件：代码编辑器，推荐[VsCode](https://code.visualstudio.com/)；[环境管理器anaconda](https://www.anaconda.com/)（可选）
2. 新建文件夹（例如dm_control），将脚本 run.py 放入文件夹中。
3. 执行命令：
```
# 创建并激活 conda 环境（示例使用 Python 3.10）
conda create -n dm_control python=3.10 -y
conda activate dm_control

# 安装 PyTorch（可以直接运行下面的命令，也可以根据官网给出的命令安装）
# 参考：https://pytorch.org/get-started/locally/
pip install torch torchvision torchaudio

# 安装其余依赖
pip install numpy imageio imageio-ffmpeg dm_control
```

注意事项（MuJoCo / dm_control）
--
- 在 VsCode 中，如果点击 run.py 右上角的运行按钮直接运行，右下角的解释器记得选择你新建的`dm_control` 的 Python 解释器；也可以在命令行中运行`conda activate dm_control`激活环境之后再运行`python run.py`。
- 渲染后端可通过环境变量 `MUJOCO_GL` 指定（`egl`, `glfw`, `osmesa`）。run.py 的默认后端是 `egl`。 `egl` 通常可用；若出现窗口或渲染问题，请尝试 `glfw` 。


运行示例
--
在激活环境并安装好依赖后，示例运行命令：
- 训练并保存视频（默认保存在 `log/<timestamp>/` 目录下）：
  ```powershell
  python run.py
  ```

